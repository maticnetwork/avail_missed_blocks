version: "3.9"

services:
  server:
    build: .
    ports:
      - "80:7000"
    links:
      - db
  db:
    image: "postgres"
    ports:
      - "5431:5432"
    environment:
      POSTGRES_PASSWORD: "1234"
      POSTGRES_USER: "postgres"
